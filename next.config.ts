import type { NextConfig } from "next";

// App Next.js completa (server) — se despliega en Hostinger como "Deploy Web App" (Node.js).
// La ruta app/api/analizar corre en el servidor y protege la ANTHROPIC_API_KEY.
const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
